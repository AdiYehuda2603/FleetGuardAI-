# ========================================
# FleetGuard AI - Environment Variables
# ========================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env (in the same directory)
# 2. Fill in your actual values
# 3. NEVER commit .env to Git (it's in .gitignore)
#
# ========================================

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
# Required for CrewAI agents to function
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI Model Selection (optional)
# Default: gpt-4o-mini (most cost-effective)
# Options: gpt-4, gpt-4-turbo, gpt-4o, gpt-4o-mini, gpt-3.5-turbo
OPENAI_MODEL_NAME=gpt-4o-mini

# Database Configuration
# Path to SQLite database (relative or absolute)
DATABASE_PATH=data/database/fleet.db

# Application Settings
# Set to 'development' or 'production'
ENVIRONMENT=development

# Streamlit Configuration (optional)
# Uncomment if you want to customize Streamlit settings
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=localhost

# Logging Level (optional)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ========================================
# Email Invoice Fetcher Configuration
# ========================================
# Automatically fetch invoice attachments from email
# Supports: Gmail, Outlook, Yahoo, any IMAP provider
#
# IMPORTANT: Use App-Specific Passwords (not regular passwords)!
#   Gmail: https://myaccount.google.com/apppasswords (requires 2FA)
#   Outlook: https://account.microsoft.com/security (requires 2FA)
# ========================================

# Enable/disable email fetching feature
EMAIL_FETCH_ENABLED=false

# IMAP Server Configuration
# Gmail: imap.gmail.com:993
# Outlook: outlook.office365.com:993
# Yahoo: imap.mail.yahoo.com:993
EMAIL_IMAP_SERVER=imap.gmail.com
EMAIL_IMAP_PORT=993

# Email Account Credentials
EMAIL_ADDRESS=your-fleet-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Email Folder/Label to Monitor
# Gmail: INBOX, [Gmail]/All Mail, or custom label name
# Outlook: INBOX, Invoices, etc.
# Other: Check your provider's folder names
EMAIL_FOLDER=INBOX

# Processing Behavior
EMAIL_MARK_AS_READ=true
EMAIL_MAX_FETCH=50
EMAIL_DATE_FILTER_DAYS=30

# ========================================
# Security Notes:
# - Keep your API keys SECRET
# - Don't share this file with real values
# - Rotate keys if accidentally exposed
# - Monitor API usage on OpenAI dashboard
# - Use App-Specific Passwords for email (NEVER regular passwords)
# ========================================
